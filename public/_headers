/*
  # Known tradeoff: 'unsafe-inline' is required because most scripts use is:inline or script
  # tags (no server-side nonce generation available on static Cloudflare Pages).
  # Migrating to nonce-gated inline scripts would require Cloudflare Pages Functions
  # for per-request nonce generation — considered but deferred; acceptable for a static site.
  Content-Security-Policy: default-src 'self'; font-src 'self' data:; img-src 'self' https:; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; connect-src 'self'
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Cross-Origin-Resource-Policy: cross-origin

# Allow caching of hashed assets
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# Data files — short cache, revalidate
*.json
  Cache-Control: public, max-age=3600, stale-while-revalidate=86400

# HTML pages — fresh always
*.html
  Cache-Control: public, max-age=0, must-revalidate

# Fonts — immutable
*.woff2
  Cache-Control: public, max-age=31536000, immutable

# Service worker — immediate update
/sw.js
  Cache-Control: no-cache

